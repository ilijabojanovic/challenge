# Copy to .env and adjust (optional — defaults match the Netlify demo).
BASE_URL=
TEST_USER_EMAIL=
TEST_USER_PASSWORD=

# Comma-separated Playwright projects: chromium | firefox | webkit
# Leave unset for Chromium-only locally.
# BROWSERS=chromium,firefox,webkit

